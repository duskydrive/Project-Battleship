import Ship from './app/ship';

const littleShip = Ship(3);
// littleShip.hit(1);
// littleShip.hit(1);
// littleShip.hit(3);
// littleShip.hit(4);
console.log(littleShip.isSunk());
// console.log(littleShip);
